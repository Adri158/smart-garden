meta {
  name: API Status
  type: http
  seq: 3
}

get {
  url: {{apiUrl}}/api/status
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("api up", function() {
    expect(res.getBody().ok).to.equal(true);
  });
}
